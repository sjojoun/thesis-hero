# 智能封面生成器 v4 改造记录

**时间：** 2026-06-16 21:40-21:48
**目标：** 双语开关 + 动态联合导师 + A4 实时预览 + 多行下划线对齐

## 改造清单

### 1. 类型扩展 (`src/types/index.ts`)
`CoverMeta` 新增 3 字段：
- `bilingual: boolean` — 双语模式开关
- `coAdvisors: string[]` — 联合指导教师列表（动态增删）
- `offCampusAdvisor: string` — 校外指导教师（选填）

### 2. 封面生成器重写 (`src/engine/coverGenerator.ts`)
**v4 新能力：**
- `bilingual` 开关：关闭时所有英文标签/字段隐藏，仅输出中文
- `coAdvisors[]` 逐行渲染：每个联合导师独立一行（对应南理工规范："如有校外指导教师联合指导，填写在下一行，没有则不填"）
- `offCampusAdvisor` 置底输出
- 空字段自动跳过（`ok()` 检查，不输出空行）
- 默认值更新 `DEFAULT_COVER_META`：bilingual=true, coAdvisors=[], offCampusAdvisor=''

### 3. 封面表单重建 (`src/components/CoverForm.tsx`)
**三段折叠布局 + 双语开关 + 动态字段：**

| 折叠区 | 内容 |
|--------|------|
| 🏫 学校 & 类型 | school, college, thesisType, thesisTypeEn(双语时显示) |
| 📝 论文题目 | title, titleEn(双语时显示) |
| 👤 作者信息 | author, studentId, major, advisor, coAdvisors(动态+/-), offCampusAdvisor |
| 📅 其他 | submitDate |

**核心交互：**
- 顶部双语开关：点击切换 `bilingual`，所有英文标签/输入框即时隐藏/显示
- 联合导师区域：`＋添加` 按钮追加空行，每行带 `✕` 移除按钮
- 空列表时灰色提示「如有多个联合导师，点击＋添加」
- 折叠区默认展开，可手动折叠

### 4. 封面实时预览 (`src/components/CoverPreview.tsx`)
**A4 CSS 仿真引擎：**
- 210mm × 297mm 白纸容器，阴影 + 边框模拟纸张
- 页边距：上30/下25/左30/右20mm

**多行下划线对齐 (`UnderlineText` 组件)：**
- 将题目文本按 38 字符/行拆分
- 每字符用 24px 固定宽度 `span` + `border-bottom` 实现下划线格子
- 多行自动换行，下划线长度完美对齐
- 空格转 `\u00A0` 保持下划线连续性

**动态渲染：**
- 双语开关联动：关闭时英文行消失
- 联合导师列表展开
- 空字段不渲染行
- 题目过长(>38字)时给出警告提示

### 5. App.tsx 集成
- 导入 `CoverPreview`，渲染在右侧栏参数面板下方
- `hasCoverInfo` 逻辑不变（仍兼容新类型）

### 6. ToolBox.tsx 更新
- 内置 coverMeta 状态新增 `bilingual`/`coAdvisors`/`offCampusAdvisor`

## 构建验证
- ✅ TypeScript 零错误
- ✅ Vite build 成功（521 modules, 5.89s）
- ✅ Dev server 启动正常 (localhost:5173)
- ⚠ SSRF 策略阻止浏览器自动化访问 → 需手动在浏览器验证

## 用户操作流程
1. 打开页面 → 默认双语开启，NJUST 模板已选中
2. 可按需**关闭双语开关** → 英文字段全部隐藏
3. 填写封面信息 → 右侧 A4 预览实时更新
4. 如有联合导师 → 点击「＋ 添加」逐行增加
5. 上传论文 → 排版 → 封面自动生成在正文前
