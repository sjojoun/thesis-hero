# 论文转换项目 v3 重构记录

**时间：** 2026-06-16 21:21-21:30
**目标：** 封面规范化、全界面中英双语、按南理工继续教育学院格式规范

## 改造清单

### 1. 类型定义 (`src/types/index.ts`)
- 新增 `CoverMeta` 接口，包含 11 个双语字段：
  school/college/thesisType/thesisTypeEn/title/titleEn/author/studentId/major/advisor/submitDate
- 新增 `NJUST_CONFIG` 模板（南理工继续教育学院 2023 版规范）：
  - 页边距：上25/下25/左30/右20 mm
  - 正文：宋体小四(12pt)/Times New Roman/1.5倍行距/首行缩进2字符
  - H1: 黑体三号(16pt)居中、H2: 黑体四号(14pt)左对齐、H3: 黑体小四(12pt)左对齐
  - 页眉：南京理工大学毕业设计说明书（论文）、页码居中
- 模板列表更新：NJUST → GB7713 → PKU → BASIC（南理工排第一）
- `AVAILABLE_CONFIGS` 增加 `NJUST_CONFIG` 为首项

### 2. 封面生成器 (`src/engine/coverGenerator.ts`) — 完全重写
- NJUST 格式双语封面布局：
  ```
  学校名称（一号 26pt 黑体居中）
  论文类型中文 + 英文（三号/四号）
  学院名称中英
  分隔线留白
  论文题目中英（小二/四号）
  学生信息双语标签（姓名/Name、学号/Student ID、班级/Major、导师/Advisor）
  日期
  [分页符] → 正文从新页开始
  ```
- 导出 `DEFAULT_COVER_META` 默认值（南理工/继续教育学院/当天日期）
- 导出 `buildCoverParagraphs(meta)` 和 `buildCoverSection(meta)`

### 3. 排版引擎 (`src/engine/formattingEngine.ts`) — v3 增强
- `processPaper` 新增第5参数 `coverMeta?: CoverMeta`
  - 传入时自动生成封面 + PageBreak 后接正文
- **章节自动分页**：检测「摘要、目录、参考文献、致谢、附录、绪论」等关键词
  - 在这些章节前自动插入 PageBreak（不影响封面后的第一个段落）
- 错误提示更新为友好中文
- 进度消息更新为中文

### 4. 封面表单组件 (`src/components/CoverForm.tsx`) — 新建
- 11 个输入字段，全部中英双语标签
- 响应式网格布局（2列/1列自适应）
- 空值友好：所有字段可留空，有则生成封面
- 与 App.tsx 通过 CoverMeta 状态双向绑定

### 5. App.tsx 改造
- 新增 `coverMeta` 状态，默认 `DEFAULT_COVER_META`
- `hasCoverInfo(meta)` 判断：题目/姓名/学号/导师任一非空即生成封面
- `handleFormat` 传递 coverMeta 给 processPaper
- 默认模板改为 `NJUST_CONFIG`
- 欢迎区文案中英双语
- 步骤指示器标签中英双语（5步：上传/模板/参数/排版/下载）
- 左侧栏新增封面信息卡片（在模板选择下方）
- Footer 添加英文副标题

### 6. UI 组件双语改造
| 组件 | 改动 |
|------|------|
| `Header.tsx` | 副标题「论文排版助手 · Thesis Hero」、状态标签「本地/Local · 零隐私/Zero-Risk」 |
| `FileUploader.tsx` | 拖拽提示、格式说明、替换文案全部中英双语 |
| `ActionBar.tsx` | 按钮文案：开始排版/Format、下载/Download、重置/Reset |
| `TemplateSelector.tsx` | 标签改为「选择排版模板 / Template」 |
| `StatusBar.tsx` | 统计标签：段/Para、章/Ch、节/Sec、图/Img、参考文献/Ref |

### 7. ToolBox.tsx 修复
- CoverMeta 导入从 `@/engine/coverGenerator` 改为 `@/types`
- coverMeta 初始状态更新为新接口（11个字段含默认值）
- `setCoverMeta` 回调参数显式类型标注

## 构建结果
- TypeScript 编译：零错误
- Vite 构建：520 模块，5.94s
- JS 体积：1,139 KB (gzip 321 KB)
- CSS 体积：28 KB (gzip 6 KB)

## 待后续
- 开发服务器需手动启动验证
- 封面的 table 表格布局 vs 居中段落可视效果待实测后微调
- 更多章节关键词分页规则可继续扩充
